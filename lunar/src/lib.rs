#![deny(unused_must_use, unnameable_types)]
#![allow(clippy::too_many_arguments, clippy::single_match)]

mod state_ctx;
mod system;
mod view;

// fn goy() -> impl View {
//     state(0, |count| )
// }
